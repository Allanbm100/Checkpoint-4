import { RxPlusCircled } from "react-icons/rx";
import { StyledButton } from "./Button.style";


export const Button = () => {
    return (
        <StyledButton>
            <RxPlusCircled size={35}/>
        </StyledButton>
    )
}